const VALIDATION_MESSAGES = {
  FILE_MISSING: 'PDF file is required',
  INVALID_FILE: 'Only PDF files are allowed',
};

const SUCCESS_MESSAGE = {
  PDF_FILE_PROCESSED: 'PDF successfully processed',
};

const PDF_CONTENT_TYPES = {
  STANDARD_FORM_CONTRACT: 'STANDARD_FORM_CONTRACT',
  PURCHASE_SALE_CONTRACT: 'PURCHASE_SALE_CONTRACT',
};

module.exports = {
  VALIDATION_MESSAGES,
  SUCCESS_MESSAGE,
  PDF_CONTENT_TYPES,
}; 